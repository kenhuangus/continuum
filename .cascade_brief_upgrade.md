# Continuum P0+P1 Upgrade Brief (implement fully)

You are upgrading Continuum in `C:\Users\kenhu\qwen-hacktoh`. Phase A demo works; close gaps from `docs/MEMORY_SAAS_ASSESSMENT.md` with REAL code (no stubs that pretend). Apache-2.0. Prefer incremental working system. Do NOT commit secrets. Do NOT git commit unless asked.

Read first: `prd.md`, `docs/MEMORY_SAAS_ASSESSMENT.md`, `packages/memory_core/**`, `apps/api/**`, `packages/mcp_server/**`, `packages/agent/**`, `tests/**`, `evals/**`, `pyproject.toml`, `README.md`, `docs/architecture.md`, `.env.example`, `apps/web/index.html`.

## DONE criteria
1. Hybrid retrieve-then-pack works offline + online (embeddings when API key present; deterministic local fallback)
2. Eval suite shows Continuum ≥ naive baselines on fixtures
3. API key auth + forget AuthZ (workspace scope)
4. MCP stdio actually serves tools wired to MemoryService
5. Unit + api + eval tests pass
6. Docs honest (architecture + README trimmed; assessment has "Progress after upgrade")

Keep web UI working: optional API key field OR `CONTINUUM_AUTH_DISABLED=1` for local demo. Update `tests/conftest.py` so api tests set `CONTINUUM_AUTH_DISABLED=1` (or inject a test key) so existing tests don't break.

---

## P0 — Scientific credibility

### 1. Hybrid retrieve-then-pack
Add `packages/memory_core/continuum_memory/embeddings.py`:
- If `DASHSCOPE_API_KEY` or `QWEN_API_KEY` present: call Qwen/DashScope embeddings API (OpenAI-compatible if repo already uses that pattern via `continuum_agent.client`). Soft-fail to local on errors.
- Offline: deterministic local embeddings — hashed bag-of-words / TF-IDF-style fixed-dim vectors (no heavy deps). Cosine similarity.
- Cache embedding vectors optionally in memory or as JSON blob column if schema allows; otherwise compute on the fly for Phase upgrade.

Add `packages/memory_core/continuum_memory/retrieve.py`:
- `retrieve_candidates(store, workspace_id, query, top_k=50, entities=None)`:
  - Sparse/keyword+entity path (existing search logic)
  - Dense path (embedding cosine)
  - Merge/dedupe union of top-K from each; return candidates only
- NEVER score entire workspace table as the only path in `MemoryService.pack`

Update `MemoryService.pack` to: retrieve candidates → pack only candidates. Keep existing pack_greedy / pack_type_quota.

### 2. Stronger supersession
- Add structured conflict slots on Memory where natural: e.g. `slots: dict[str, Any]` field (or encode in `source["slots"]` / new schema field). Prefer adding `slots: dict[str, Any] = {}` to Memory schema.
- Extract slots where possible: `discount_pct`, `vip` (bool), `contact_channel`, entity name, etc. in extractor heuristics (domain-agnostic patterns, not only Acme).
- In supersession: if same entity+same slot key with different values → conflict. Keep heuristic topic fallback.
- Allow multiple supersedes: change `supersedes: str | None` to `supersedes: list[str] = []` (or keep str for single + add `supersedes_ids` JSON). Prefer `supersedes: list[str]`. Migrate SQLite carefully: store as JSON array string; on read parse both legacy single string and JSON list. Update `mark_superseded` to APPEND to supersedes list on new memory, not overwrite.

### 3. Extract + critic
- Skip writing memories from pure interrogatives (already partially done — strengthen).
- Domain-agnostic heuristics: VIP/discount/preference/SLA/owner patterns for any CapWords entity, not only Acme hardcodes.
- When LLM available: extract then critic/validate pass (`extract_with_llm` + `critique_memories`) that drops questions, hallucinations, duplicates, low-confidence junk. Fall back to heuristic if critic fails.
- Mark provenance in `source`: extractor type, critic passed, etc.

### 4. Eval suite
Expand beyond single Acme fixture:
- Add ≥15–20 solid fixture JSON files under `evals/fixtures/` (diverse domains: SaaS pricing, ITSM SLA, consulting prefs, healthcare scheduling prefs, e-commerce VIP, etc.). Each fixture: session_a turns, session_b query, critical_facts[], stale_facts[], budget_tokens.
- Create `evals/run_suite.py` and `packages/memory_core/continuum_eval/` OR `continuum_eval` module so `python -m continuum_eval` works (add package under packages/ or evals as module). Prefer `evals/continuum_eval/` with `__main__.py` and entry in pyproject if easy: `python -m continuum_eval` or `python evals/run_suite.py`.
- Baselines: `no_memory`, `full_history_dump`, `naive_topk_keyword`, `continuum_pack` (hybrid retrieve+pack).
- Metrics: critical-fact recall@budget, stale leakage after supersession, token use.
- Keep `evals/run_smoke.py` working.
- Add pytest markers: `eval` for suite tests under `tests/eval/` or invoke from `evals/`.
- Aim Continuum ≥ naive on aggregate recall and lower stale leakage.

### 5. Docs
- Update `docs/architecture.md` to describe hybrid retrieve-then-pack, auth, MCP, store interface honestly (what's shipped vs later).
- Trim README overclaims; document auth env vars, MCP run command, eval suite run.
- Append section to `docs/MEMORY_SAAS_ASSESSMENT.md`: **"Progress after upgrade"** listing what closed vs still open.

---

## P1 — SaaS blockers

### 1. API key auth
- Env: `CONTINUUM_API_KEYS` (comma-separated) and/or `CONTINUUM_AUTH_DISABLED=1` for local demo.
- Middleware/dependency: require `X-API-Key` or `Authorization: Bearer <key>` on mutating/sensitive routes. Health stays open. GET pack_preview / list can require key when auth enabled (simpler: protect all except `/v1/health` and `/` static).
- Document in `.env.example`.

### 2. AuthZ / IDOR fix
- `POST /v1/memories/{id}/forget` MUST accept `workspace_id` query/body and verify memory belongs to that workspace before forget. Return 404 if wrong workspace or missing.
- `GET` memory by id similarly scoped if exposed.
- Update `MemoryService.forget(memory_id, reason, workspace_id=...)`.

### 3. Store abstraction
- Add `packages/memory_core/continuum_memory/store_base.py` (Protocol/ABC) with methods used by service.
- Keep `MemoryStore` as SQLite impl.
- Optional Postgres: if `DATABASE_URL` set and starts with `postgres`, use SQLAlchemy Postgres impl. Add `sqlalchemy` to dependencies if implementing Postgres. If time-tight: solid interface + SQLite; stub Postgres with clear NotImplemented or minimal SQLAlchemy that works for smoke. Prefer real SQLite + clean interface; Postgres optional but try.

### 4. Real MCP server
Replace stub in `packages/mcp_server/continuum_mcp/server.py` with working stdio MCP server.
- Prefer official `mcp` Python SDK if available via pip; else implement minimal JSON-RPC stdio loop for tools/list and tools/call.
- Tools: search, remember, forget, list, explain, pack_preview — wired to MemoryService.
- Entry: `python -m continuum_mcp` or documented console script.
- Update `packages/mcp_server/README.md`.

### 5. Quotas / rate limit
Simple in-memory (or DB) per-API-key or per-workspace counters: max requests/minute. Env `CONTINUUM_RATE_LIMIT_RPM=60` default. Return 429 when exceeded.

### 6. Idempotency
Optional `Idempotency-Key` header for `POST /v1/memories` and `POST /v1/chat`. Cache responses briefly in-memory by key.

### 7. Observability
- Request ID middleware: generate or accept `X-Request-Id`; echo in response header.
- Structured log lines (json or key=value) for pack: hit counts, candidate count, packed count, tokens.

### 8. Harden
- CORS: remove `"null"` unless needed; keep localhost origins.
- Memory injection: when building packed context block for agent, mark provenance and strip obvious injection patterns (`ignore previous instructions`, etc.).

---

## P2 if bandwidth (ship if natural)
- `pack_knapsack_dp` and/or `pack_mmr` in packer.py; wire algorithm names.
- `as_of` optional datetime filter on pack/list (effective_from/to).
- HITL flag: high-confidence forget can set `policy_tags` / require confirm — simple flag `hitl_required` on forget response for high-utility memories OR env gate.
- Utility bump on successful pack use (already have update_utility — bump slightly when packed).

---

## Tests / conftest
- Update `tests/conftest.py` `api_client` to set `CONTINUUM_AUTH_DISABLED=1` before reload.
- Update forget API tests to pass workspace_id.
- Add unit tests: retrieve hybrid, supersession slots, multi-supersedes, extractor questions rejected, packer knapsack if added.
- Add api tests: auth 401 without key when auth enabled; forget IDOR returns 404 for wrong workspace.
- Integrate with existing `tests/unit`, `tests/api`, `tests/e2e` layout.
- Add `eval` marker in pyproject.toml.

## Web UI
- Add optional API key input in controls; send as `X-API-Key` header on fetch calls.
- If empty and server has auth disabled, works as today.

## pyproject.toml
- Add deps as needed: `sqlalchemy` (if Postgres), `mcp` (if using SDK).
- Ensure packages discoverable; add continuum_eval if created under packages.
- Add console script or document module run paths.

## Implementation order
1. schemas + store migration (slots, supersedes list) + store_base
2. embeddings + retrieve + service.pack hybrid
3. supersession slots + extractor+critic
4. packer P2 algorithms if quick
5. API auth, AuthZ, rate limit, idempotency, request id, CORS, injection sanitize
6. MCP real server
7. Eval fixtures + run_suite
8. Docs + .env.example + UI API key field
9. Fix all tests

After implementing, run:
```
pytest -m unit -q
pytest -m api -q
python evals/run_smoke.py
python evals/run_suite.py
```
Fix failures. Summarize what you shipped.
