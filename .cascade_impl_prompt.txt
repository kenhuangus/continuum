Implement the FULL Continuum P0+P1 upgrade. Read and follow COMPLETELY:

  C:\Users\kenhu\qwen-hacktoh\.cascade_brief_upgrade.md

Also read before coding: prd.md, docs/MEMORY_SAAS_ASSESSMENT.md, packages/memory_core/**, apps/api/**, packages/mcp_server/**, packages/agent/**, tests/**, evals/**, pyproject.toml, README.md, docs/architecture.md, .env.example, apps/web/index.html.

DONE criteria (all required):
1. Hybrid retrieve-then-pack offline+online (embeddings when API key present; deterministic local hashed bag-of-words / TF-IDF-style fixed-dim vectors + cosine otherwise). Soft-fail to local on API errors.
2. Eval suite Continuum >= naive baselines on fixtures
3. API key auth + forget AuthZ (workspace scope)
4. Real MCP stdio wired to MemoryService (not a stub that prints schemas and exits)
5. Unit + api + eval tests pass
6. Docs honest

CRITICAL CONSTRAINTS:
- Do NOT git commit. Do NOT commit secrets. Apache-2.0.
- Real code only — no stubs that pretend to work.
- Prefer incremental working system.
- Keep web UI working: optional API key field OR CONTINUUM_AUTH_DISABLED=1 for local demo.
- Update tests/conftest.py api_client to set CONTINUUM_AUTH_DISABLED=1 before reload so existing tests don't break.
- Update forget API tests to pass workspace_id.
- supersedes: change to list[str] = []; migrate SQLite carefully (store JSON array; on read parse legacy single string AND JSON list). mark_superseded APPENDS to supersedes list.
- MemoryService.pack MUST retrieve candidates first then pack only candidates — NEVER score entire workspace as only path.
- Forget MUST require workspace_id and verify memory belongs to workspace (404 otherwise).
- Protect all API routes except /v1/health and / static when auth enabled; X-API-Key or Authorization: Bearer.
- CONTINUUM_API_KEYS comma-separated; CONTINUUM_AUTH_DISABLED=1 for demo; CONTINUUM_RATE_LIMIT_RPM=60.
- Add slots: dict[str, Any] = {} to Memory schema.
- Domain-agnostic extractor heuristics for CapWords entities (VIP/discount/preference/SLA/owner) — not only Acme hardcodes. Reject pure interrogatives. LLM path: extract + critique_memories critic; fall back to heuristic if critic fails.
- Store abstraction Protocol/ABC in store_base.py; MemoryStore remains SQLite impl. Postgres optional if DATABASE_URL starts with postgres — prefer clean interface + SQLite if time-tight.
- Eval: >=15–20 fixture JSON files under evals/fixtures/; evals/run_suite.py + evals/continuum_eval/; baselines no_memory, full_history_dump, naive_topk_keyword, continuum_pack; metrics critical-fact recall@budget, stale leakage, token use. Keep run_smoke.py working. Add pytest marker eval.
- P2 if natural: pack_knapsack_dp and/or pack_mmr; as_of filter; HITL flag; utility bump on pack.
- CORS: remove "null"; keep localhost. Sanitize memory injection patterns. Request ID middleware. Idempotency-Key for POST memories/chat. Structured pack logs.
- Update docs/architecture.md, README.md, append "Progress after upgrade" to MEMORY_SAAS_ASSESSMENT.md, .env.example.
- pyproject.toml: add mcp / sqlalchemy if used; eval marker; pythonpath for packages.

Implementation order from the brief. After implementing, run and FIX until green:
  pytest -m unit -q
  pytest -m api -q
  python evals/run_smoke.py
  python evals/run_suite.py

Summarize what you shipped when done. Do not leave stubs. Do not commit.
